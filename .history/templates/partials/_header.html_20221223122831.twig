<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{path("home.index")}}"> 
     SupaMarket
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{path("product")}}">Nos produits
          </a>
        </li>
        {% if app.user %}
        <a href="{{path('security.logout')}}" class="nav-link active" ><button class="btn btn-danger">Deconnexion</button></a>
      
        {% else %}
           <a href="{{path('security.login')}}" class="nav-link active" ><button class="btn btn-success">Connexion</button></a>
        {% endif %}
        
        {% if 'ROLE_ADMIN' in app.user.roles %}
         <a href="{{path('admin')}}" class="nav-link " >Administration</a>
        {% endif %}
          </ul>
      </div>
  </div>
</nav>